{
  "rules": {
    // Výchozí pravidla - zamítnout vše
    ".read": false,
    ".write": false,

    // Audio metadata - veřejné čtení, zápis pouze pro admin
    "audio-metadata": {
      ".read": true,
      ".write": "auth != null && auth.token.admin === true"
    },

    // Audio statistiky - veřejné čtení, zápis pouze pro admin
    "audio_stats": {
      ".read": true,
      ".write": "auth != null && auth.token.admin === true"
    },

    // Test data - pouze pro autentifikované
    "test": {
      ".read": "auth != null",
      ".write": "auth != null"
    },

    // Metadata - veřejné čtení, zápis pouze pro admin
    "metadata": {
      ".read": true,
      ".write": "auth != null && auth.token.admin === true"
    },

    // System data - pouze pro admin
    "system": {
      ".read": "auth != null && auth.token.admin === true",
      ".write": "auth != null && auth.token.admin === true"
    },

    // User data - každý uživatel může číst/zapisovat pouze své vlastní data
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },

    // UI data - veřejné čtení (texty, překlady, konfigurace), zápis pouze pro admin
    "ui-data": {
      ".read": true,
      ".write": "auth != null && auth.token.admin === true"
    }
  }
}